language: node_js
node_js: stable

install:
- npm install

script:
- npm build

after_script:
- cd ./dist
- git init
- git config user.name "yfgeek"
- git config user.email "yifanb@gmail.com"
- git add .
- git commit -m "Update docs"
- git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master

branches:
  only:
  - source
env:
  global:
  - GH_REF: github.com/yfgeek/paper-manager-vue.git